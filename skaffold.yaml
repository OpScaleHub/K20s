apiVersion: skaffold/v4beta13
kind: Config
metadata:
  name: k20s
build:
  local:
    push: false
  artifacts:
  - image: ghcr.io/opscalehub/k20s # This will be updated by the release workflow
    context: .
    docker:
      dockerfile: Dockerfile
manifests:
  kustomize:
    paths:
      - config/default
